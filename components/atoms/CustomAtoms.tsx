import { Col, Row } from "react-bootstrap";
import { styled } from "styled-components";

export const ScheduleRow = styled(Row)`
  min-height: 30px;
`;

export const SignInGroupButtonRow = styled(Row)`
  min-height: 120px;
`;

export const CenterCol = styled(Col)`
  text-align: center;
  margin: auto;
`;